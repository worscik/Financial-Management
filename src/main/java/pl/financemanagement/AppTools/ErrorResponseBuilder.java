package pl.financemanagement.AppTools;

import org.springframework.validation.BindingResult;
import pl.financemanagement.Expenses.Model.ExpenseResponse;

import java.util.HashMap;
import java.util.Map;
//TODO global api error reposne
public class ErrorResponseBuilder {

//    public static ExpenseResponse buildErrorResponse(BindingResult result) {
//        Map<String, String> errors = new HashMap<>();
//        result.getFieldErrors().forEach(error -> errors.put(error.getField(), error.getDefaultMessage()));
//        return new ExpenseResponse(false, errors);
//    }

}
